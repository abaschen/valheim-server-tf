[
    {
      "name": "${app}",
      "image": "${image}",
      "essential": true,
      "portMappings": ${ports},
      "memory": ${memory},
      "cpu": ${cpu},
      "environment": ${envs}
    }
  ]